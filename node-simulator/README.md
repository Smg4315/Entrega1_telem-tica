# node-simulator/ — Simulador de nodos NMP

Simula uno o varios nodos que hablan NMP real contra `server/` (REGISTER, STATUS periódico,
EVENT) para poder demostrar el flujo completo sin hardware físico.

## Estado

Sin implementar. Puede reusar `gateway/src/nmp/nmpParser.ts` (mismo formato NMP).
