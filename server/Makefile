# TODO: reglas de compilación reales una vez existan los .c con lógica.
CC = gcc
CFLAGS = -Wall -Wextra -std=c11
SRC = $(wildcard src/**/*.c)

all:
	@echo "Pendiente: implementar reglas de build sobre $(SRC)"
